# Logarift Frontend

React + Vite frontend for local manual friction logging and dashboarding.

Run directly:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8080` by default.
Override with:

```bash
VITE_LOGARIFT_API_BASE_URL=http://localhost:8080 npm run dev
```
